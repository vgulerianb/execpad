# execpad (Python)

This directory is the **Python** distribution of [execpad](../README.md). The **canonical README** (install for npm + pip, quick starts, features, and docs index) lives at the **repository root**: [`../README.md`](../README.md).

```bash
pip install -e ./python   # from clone
pytest                    # with pip install -e '.[dev]'
```

See also [docs/python.md](../docs/python.md).
